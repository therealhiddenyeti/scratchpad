---
- hosts: localhost

  tasks:
  - name: Get py version
    command: /usr/bin/python3 --version

#  - name: execute parser
  - local_action: 
#      module: command /usr/bin/python3 /tmp/transform.py -i /tmp/response.json -o /tmp/parsedPolicy.csv
      module: script /tmp/helloworld.sh

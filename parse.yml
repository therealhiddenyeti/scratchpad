---
- hosts: localhost
#  become: yes
#  become_user: root
#  gather_facts: no

  tasks:
  - name: "BASIC FILTER"
    command: cat /tmp/response.json | json_query('results[*].policyid')
#      msg: "{{ result['content-updates'] | json_query('entry[*].version') }}"
#      msg: cat /tmp/response.json | json_query('results[*].policyid')

#   tasks:
#   - name: Get py version
#     command: /usr/bin/python3 --version

#   - name: execute parser
#   - local_action: 
#     command: /usr/bin/python3 /tmp/transform.py -i /tmp/response.json -o /tmp/parsedPolicy.csv
#     command: whoami
#     shell:  /tmp/helloworld.sh
